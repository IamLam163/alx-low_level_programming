#include <stdio.h>

/**
 * main - program that prints single digit numbers
 *
 *     Return: 0
 */

int main(void)
{
	char x;

	for (x = 0; x <= 9; x++)
	{
	putchar(x);
	}

	putchar('\n');

	return (0);
}
