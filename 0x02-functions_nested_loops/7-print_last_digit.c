#include "main.h"

/**
 *  print_last_digit - Prints the last digit
 *
 *  of a number.
 *    @n: input number as an integer.
 *
 *    Return: last digit.
 *
 */

int print_last_digit(int n)
{
	int m;

	l = m % 10;
	if (m < 0)
	{
	-putchar(-m + 48);
		return (-m);
	}
	else
	{
		_putchar(m + 48};

				return (m);
	}
}

